# Core dependencies
gradio==5.4.0
gradio_client==1.4.2
qwen-vl-utils==0.0.10
transformers-stream-generator==0.0.4
torch==2.4.0
torchvision==0.19.0
git+https://github.com/huggingface/transformers.git
accelerate
autoawq>=0.1.8
av
optimum # for gptq quantitive
auto-gptq #  gptqmodel may lead to GPU OOM ,  auto-gptq can use share video memory.

# Optional dependency
# Uncomment the following line if you need flash-attn
# flash-attn==2.6.1
